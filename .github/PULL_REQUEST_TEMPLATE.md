## Summary

<!-- 1-3 bullet points: what does this PR do and why. -->

## Changes

| File | Change |
|---|---|
| | |

## Verification

<!--
No pytest/CI-only test suite exists for the full pipeline (model weights
and Postgres/Supabase credentials aren't available in CI). State what you
actually ran:
- `python -m py_compile` / `python -m compileall`
- Relevant `tests_manual/*.py` scripts (see their own docstrings for what
  each one covers and what it needs installed)
- Any manual `run_local.py` / real-data check
-->

- [ ] `python -m compileall camerachatbot` passes
- [ ] Relevant `tests_manual/*.py` scripts pass
- [ ] Manually verified the affected behavior (describe how)
